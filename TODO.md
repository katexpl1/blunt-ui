New stuff to add

- [ ] ConfirmDialog — no "are you sure?" flow. Could be a useConfirm() hook that opens Modal with yes/no buttons
- [ ] DatePicker component

---

Form / useForm fixes

- [x] warn in dev when name prop is missing on a field
- [x] reset(newValues?) — populate form from async data without calling setFieldValue per field
